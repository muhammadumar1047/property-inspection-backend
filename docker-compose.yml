services:
  backend:
    build:
      context: ./backend
    container_name: backend
    restart: unless-stopped
    ports:
      - "5000:80"
    environment:
      - ConnectionStrings__Default=Host=ec2-52-62-164-129.ap-southeast-2.compute.amazonaws.com;Port=5432;Database=propertydb;Username=postgres;Password=postgres